{
  "name": "verbos",
  "version": "1.0.0",
  "description": "An application with hundreds of regular and irregular verbs that will help you understand their different conjugations.",
  "repository": {
    "type": "git",
    "url": "ssh://git@codeberg.org/brargenzilian/verbos.git"
  },
  "license": "GPL-3.0",
  "author": "Brargenzilian",
  "type": "commonjs",
  "private": "true",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile-ts": "tsc --build",
    "copy-html": "cp ./src/index.html ./docs && cp -r ./src/views ./src/assets ./docs",
    "build": "npm run copy-html && npm run compile-ts"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
