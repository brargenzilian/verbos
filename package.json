{
  "name": "verbos",
  "version": "1.0.0",
  "description": "An application with hundreds of regular and irregular verbs that will help you understand their different conjugations.",
  "repository": {
    "type": "git",
    "url": "ssh://git@codeberg.org/brargenzilian/verbos.git"
  },
  "license": "GPL-3.0",
  "author": "Brargenzilian",
  "type": "commonjs",
  "private": "true",
  "scripts": {
    "clean": "rm -rf docs/*",
    "compile-ts": "tsc --build",
    "copy-css": "cp ./src/style.css ./docs",
    "copy-assets": "cp -r ./src/assets ./docs",
    "copy-html": "cp ./src/index.html ./docs && cp -r ./src/views ./docs",
    "build": "npm run clean && npm run copy-assets && npm run copy-css && npm run compile-ts && npm run copy-html"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
